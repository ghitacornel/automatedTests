how to build custom test setup

annotations used here can be seen as small helpers that allow a standard way of coding

for multiple methods annotated with before/after the order of invocation is not specified 
hence no assumptions can be made

inheritance seems not to be supported